import { MutualfundPipe } from './mutualfund.pipe';

describe('MutualfundPipe', () => {
  it('create an instance', () => {
    const pipe = new MutualfundPipe();
    expect(pipe).toBeTruthy();
  });
});
